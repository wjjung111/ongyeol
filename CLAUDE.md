# CLAUDE.md — Ongyeol v1.0 프로젝트 지침서

> Claude Code가 이 저장소에서 작업할 때 먼저 읽는 파일.
> Ongyeol v1.0 = 온결 감정평가법인 집합건물 **감정평가서·청구서 한글(.hwpx) 자동화**.
> 구 `wjjung111/woojin`의 EXACT 집합건물 v2.0을 전용 저장소로 분리한 것.

## 1. 목적

감정평가사가 집합건물 1건의 **감정평가서(표지+감정평가표+산출근거) + 청구서**를 한글파일로 즉시 뽑도록 자동화. 평가사 판단(사례 선정·요인 우열·최종 결정단가)은 자동화하지 않는다.

## 2. 구조

```
ongyeol/
├── index.html          ← 입장 화면(허브): EXACT 집합건물 / EXACT 토지건물(준비중) / 시점수정 계산(집합건물·토지) 버튼
├── 집합건물.html        ← ★집합건물 앱 본체 (단일 HTML, React+Babel CDN, 한글 템플릿 base64 내장. 구 index.html)
├── 토지조회.html        ← 토지 조회 도구 (토지건물 준비작업, 브이월드 API)
├── 건축물대장.html      ← 건축물대장 조회 (지번 → V-World PNU → 건축HUB 표제부·층별·지역지구, 프록시 경유). 같은 지번이 여러 동네면 선택 목록. 집합건물이면 동→호 고르면 전유·공용·계약면적·전용률 (전유부·전유공용면적 API — **프록시 2판 이상 필요**, 구판이면 화면에 재배포 안내)
├── 문서/클라우드플레어_프록시.js      ← 프록시(Cloudflare Worker) 원본. 바뀌면 사용자가 대시보드에 붙여넣어 재배포해야 적용 (절차: 문서/클라우드플레어_프록시_설정.md 6번)
├── 관리자.html          ← 사용 기록 조회 (숨은 페이지, 입장 화면에 링크 없음). 비밀번호는 수신함이 검증. 탭 2개: 사용 기록('기록' 시트) / 시점수정 접속('접속' 시트: IP·지역·기기)
├── test.html           ← 개발/테스트 허브 + 관리자 조회(관리자.html과 같은 로직·탭을 복사해 둠 — **관리자 화면 고칠 땐 두 파일 함께**). 사용자가 실제로 쓰는 관리자 화면은 이쪽
├── arap_access.js      ← ★접속코드 잠금 (온결 평가사 전용). 허브·집합건물·입주권·토지건물·토지조회·test.html 등 내부 화면 `<body>` 바로 아래에서 로드. 브라우저당 최초 1회 코드 입력, 파일엔 SHA-256 해시만 둠(코드 변경 = 해시 교체). 건축물대장.html·arm_시점수정.html(이름 게이트)·관리자.html에는 붙이지 않음
├── arap_name.js        ← ★사용자 이름 게이트 (접속코드 없음). 건축물대장.html·arm_시점수정.html `<body>` 바로 아래에서 로드. 브라우저당 최초 1회 이름을 자유 입력(2자 이상), 입력 전엔 화면이 안 열림. 저장 키는 집합건물과 같은 `arap-user-name`(그 앱에서 고른 이름이면 그대로 통과). 상단바 `#arap-name-badge` 클릭으로 이름 변경, `?user=이름`으로 바로 저장
├── 시점수정.html        ← 옛 주소 → arm_시점수정.html 자동 이동용 (외부 공유된 링크 유지)
├── arm_시점수정.html    ← 시점수정 계산기 (외부 공개, 이름 게이트). 이름 확정 뒤 접속·계산 때 IP 조회 서비스(ipwho.is→ipapi.co)로 받은 IP·지역 + 기기·브라우저 + 이름을 수신함 '접속' 시트로 전송
├── 문서/관리자_수신함.gs      ← 기록을 받아 구글 시트에 쌓는 앱스 스크립트 원본 (관리자 구글 계정에 붙여넣어 배포)
├── 문서/관리자_수신함_설정.md ← 위 스크립트 설치·배포·점검 절차
├── arap_index_data.js  ← 매매가격지수 데이터 (자동생성 — 직접수정 금지). 집합건물.html이 상대경로로 로드
├── arap_launch.ps1     ← 부동산원 R-ONE API 지수 수신 (로컬/Actions용)
├── .github/workflows/update-index.yml ← 지수 자동갱신 (매일 06:00 KST, Secret RONE_API_KEY 필요)
├── 템플릿/             ← v2 hwpx 원본 (감정평가서·청구서) — 런타임 미사용, 재생성용 보관
└── 문서/               ← 플레이스홀더 매핑·작업이력·**결정기록.md**(설계·UX 결정과 기각안, Claude가 PR마다 추가)
```

## 3. 작업 원칙 (필수)

- **작업 = 새 브랜치 → 수정 → PR → main 머지.** **머지는 Claude가 항상 직접 수행**(사용자가 별도로 안 물어봐도 PR 만든 뒤 바로 머지). main 머지 시 Pages 자동 재배포.
- 코드 수정 후 "끝"이라 하지 말고 **실제 다운로드 → 한글에서 열어 확인**까지. hwpx 생성은 코드리뷰로 검증 불가.
- 사용자는 코딩을 잘 모름 — **한국말·결과 위주**로 설명, 할 일은 숫자로 명시. 모르면 추측 말고 묻는다.
- 위험 작업(템플릿 교체·localStorage 키 변경·대규모 리팩토링)은 사전 동의.
- **결정기록(`문서/결정기록.md`)을 Claude가 관리한다.** 세션에서 사용자가 내린 설계·UX 결정(채택안, 이유, 기각한 대안, 보류 사항)을 **PR 머지 때마다** 그 파일에 두세 줄씩 추가해 같은 PR에 포함한다. 새 세션은 관련 작업 전에 이 파일을 먼저 읽고, 이미 정해진 것을 다시 묻지 않는다. 사용자는 따로 쓰지 않는다.
- **모든 입력칸은 사용자가 자유수정 가능해야 한다** — 자동계산 값도 칸에 채워주는 방식으로 구현하고 readOnly 금지 (특별용역비·여비 등).
- **사용자 이름은 허용 목록에서만 선택** — 첫 접속 게이트가 드롭다운으로 이름을 고르게 하고, 목록 밖 이름(옛 자동번호 `사용자-XXXX` 포함)은 무효 처리해 게이트를 다시 띄운다. 이름이 없으면 사용기록도 보내지 않는다. 목록은 `window.ARAP_USERS`(드롭다운에 보임)와 `window.ARAP_HIDDEN_USERS`(관리자 wdw — 드롭다운엔 안 보이고 저장돼 있으면 통과, 새 기기는 `?user=wdw` 한 번)로 나뉘며 집합건물.html·집합건물_test.html·입주권.html 상단 스크립트 **세 파일 모두 같은 값으로** 유지. 저장 키 `arap-user-name`은 그대로.

## 4. hwpx 템플릿·엔진 (핵심)

- 한글파일 생성 = **hwpx(zip+XML) 템플릿 안 `{{토큰}}` 문자열 치환** 방식. `buildTokenHwpx`(공용), `buildEvalHwpxV2`(감정평가서), `buildCheongguTokenMap`+`buildTokenHwpx`(청구서).
- 플레이스홀더는 템플릿에 **파란색**으로 표시(검토용). 앱이 값 채운 뒤 파란 글자모양(charPr)을 원래 검정으로 되돌림(`V2_BLUE2BLACK`/`V2CHEONGGU_BLUE2BLACK`) → **완성본은 전부 검정**. (한글은 전체선택 글자색변경으로 charPr 색을 못 바꿔서 이렇게 처리)
- 템플릿 재생성 스크립트는 개발 세션 scratchpad에 있음(`finalize_v2b.py`, `make_cheonggu_template.py`, `build_v2_app.py`). 토큰 매핑 상세: `문서/의견서v2.0_플레이스홀더_매핑.md`.

## 5. 위험 지점 (Landmines)

| 위치 | 문제 | 대응 |
|---|---|---|
| 표 계산식(FORMULA) 필드 | 캐시된 샘플값이 남고 한글 재계산이 플레이스홀더를 덮어씀 | 템플릿 만들 때 fieldBegin/fieldEnd 쌍 제거 |
| 꼬리말(footer) 낀 런 분리 | 감싸는 런 charPr을 rfind로 찾으면 꼬리말 내부(굴림9pt)를 잘못 집음 | 여닫이 **스택**으로 실제 바깥 런 탐색 |
| linesegarray(줄배치 캐시) | 값 셀에 긴 토큰 넣으면 겹침 / 전역 제거하면 본문 재배치 | **값 셀(짧은 문단)만** 제거, 본문 산문은 유지 |
| localStorage 키·`arap_*`·`ArapSync` | 개명 시 사용자 저장데이터·지수 연결 끊김 | **절대 개명 금지** (표시 이름만 Ongyeol) |
| charPr id 불연속·itemCnt 불일치 | 한글은 글자모양을 id가 아닌 **목록 순번**으로 찾음 — id에 구멍(예: 53~55 없이 56·57·58)이 있으면 범위 밖 참조가 되어 **기본 세리프 글꼴로 대체**됨(입주권 의견서 폰트 깨짐의 원인). itemCnt≠실제 개수면 손상 경고 위험 | 검정 쌍둥이 charPr은 **빈 번호에 순서대로** 추가, id 연속·itemCnt 일치 유지 |
| 대표 샘플 `V2_SAMPLE` | 저장키 `SAMPLE-압구정현대`(실접수번호와 충돌 방지), 표시 접수번호는 `caseNoDisplay` | `_sampleVer`로 구버전 샘플 자동갱신, 실사용자 건(SAMPLE- 아님)은 미개입 |

## 6. 대표 샘플

압구정동 현대아파트 77동 904호 (접수번호 표시 HI260709-2702). 첫 방문 시 자동 로드. 물건정보·소유자·청구서 값(착수금 100만·특용비 0·하한요율·여비 10만 → 총계 585.2만)은 공부서류·접수글 기준 실값. 거래사례 시점수정 지수는 예시값.

## 7. 남은 작업 후보

- 표지·회보서 한글(hwp)화 (현재 미제공 — 양식 받으면)
- 한글 2010용 .hml 병행 (한글 2014 미만 사용자용)
- 여러호수·비주거용 v2 양식

## 8. 모르는 건 묻는다

이 문서에 없는 상황은 추측 말고 사용자에게 **결과 위주 2~3개 선택지**로 묻는다.

---
**분리 이력**: 2026-07-25 woojin 저장소에서 Ongyeol v1.0 전용 저장소로 분리.

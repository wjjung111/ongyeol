# 결정기록 (ADR-lite)

> 코드와 PR은 "무엇이 바뀌었나"를 남기지만 "왜 그렇게 정했나, 무엇을 기각했나"는 남기지 않는다.
> 이 파일은 그 빈칸을 메운다. **Claude가 PR을 머지할 때마다 그 세션에서 내린 결정을 여기에 추가한다**(CLAUDE.md 3번 원칙).
> 사용자는 따로 쓰지 않는다. 한 결정에 두세 줄. 형식: 날짜 · 결정 · 이유 · 기각한 대안(있으면) · 관련 PR.
> 새 세션의 Claude는 같은 질문을 다시 묻기 전에 이 파일을 먼저 읽는다.

## 2026-09-04 · 토지건물 (공시지가기준법·거래사례비교법)

- **칸 색은 "여기서 고칠 수 있느냐"로만 나눈다.** 노랑 = 평가사가 정하는 값, 연한 회색 = 자동이지만 직접 고칠 수 있음(고치면 파란/빨간 글씨), 진한 회색 = 자동이고 고칠 수 없음(개별요인 '계' 등). 패널 종류로 색을 다르게 하지 않는다. *기각*: 시산가액 패널만 연하게 두자는 안 — 규칙이 깨져 사용자가 클릭해 보게 됨. 시산가액 패널 등 다른 고정 칸에 진한 회색을 확대 적용하는 건 **보류**(사용자 판단 대기). PR #85 #94
- **그 밖의 요인 표의 자동 칸은 전부 직접 수정 가능.** 값이 자동계산과 다르면 빨간 글씨 + 경고띠 + 「↺ 원래대로 복원」. 수정값은 표준지별 `STDS[j].etcOvr`에 저장. 아래 개별요인비교치 '계'는 항상 자동값으로 두어 비교 기준 역할. PR #85
- **시점수정은 사례 기준으로 계산한다.** 지역·용도는 본건이 아니라 선택한 사례의 소재지·용도지역. 지가변동률 자동조회를 먼저 누르지 않아도 사례를 고르면 표를 스스로 준비해 계산. 각주에 근거를 남긴다: `1.00443 — 서울 중랑구 (26.01.31~26.04.14) (주거지역)`. PR #88
- **사례 소재지에 시군구가 없으면("상봉동 74-6") 주소 조회로 시군구를 찾는다.** 후보가 여럿이면 본건과 같은 시·도 우선, 못 찾으면 본건 시군구. **"(추정)" 같은 라벨은 붙이지 않는다** — 사용자는 당연히 본건 근처 사례를 넣고, 잡다한 표시는 원치 않음. 접미사를 뗀 한 글자 조각(동구→동)은 매칭에서 무시(상봉동이 대전 동구로 잡히던 오류). PR #91
- **시점수정치 조회는 사례를 "선정"할 때만.** 결과는 사례 객체(`timeMeta`)에 저장돼 다른 사례로 옮겨 다녀도 계속 보이고, 같은 기간이면 재선정 시 재조회 없음. ↻ 버튼만 강제 재조회. 거래일자·소재지·용도지역을 고치면 그 사례 값만 지움. *기각*: 그 밖의 요인 탭에 로딩 표시만 넣는 A안 — 거래사례 탭에서 선정하면 아무것도 안 보임. PR #89
- **거래사례 표에 '시점수정치' 행을 두고, 평가사례 표에는 (일단) 열을 두지 않는다.** 사용자가 "안 붙여도 될 듯"으로 보류. 단, 같은 개념은 같은 자리에 두는 것이 맞으므로 **통일 방향(평가사례에도 숫자 굵게 + 근거 작은 글씨로 압축한 열)을 제안한 상태**. 다음에 붙일 때는 이 형식으로. PR #89 #98
- **거래사례비교법 시점수정은 거래사례 표에서 계산해 둔 값을 끌어온다.** 탭을 열 때 칸이 비어 있으면 채우고, 손으로 적은 값은 덮지 않는다. 채택을 바꾸면 항상 그 사례 값으로. 보정표 아래 근거줄 + ↻ 버튼. PR #96
- **공시지가기준법 시산가액 카드는 왼쪽 열(개별요인비교 아래).** 거래사례비교법 탭과 같은 구조로 맞춤. 11칸이라 왼쪽 열에서는 옆으로 스크롤됨(면적·시산가액만 남기는 축약은 미결). PR #96
- **거래사례 ✕(비우기/삭제)는 실행취소가 있어야 한다.** 복구 버튼은 안내줄이 아니라 **지운 열 머리글의 ✕ 옆 ↩**. 여러 번 누적, 선정·채택 상태도 함께 복원, 새로고침하면 사라짐. *기각*: 안내줄의 「↩ 복구」 버튼 — 사용자가 ✕ 옆을 원함. PR #96 #98
- **평가사례도 거래사례처럼 행마다 붙여넣기 칸(kapa hub).** 상단 큰 붙여넣기 상자는 없앰. 붙이면 그 행부터 채우고 여러 건은 아래 행으로. 기본 빈 3행, ✕는 3행까지 비우기. PR #97
- **예시 회색글씨(placeholder)는 본건 토지가 조회되면 전부 숨긴다.** "예: 안양시 동안구 …" 같은 예시가 실제 물건(상봉동)과 달라 혼란을 줌. '-'·'반올림'·'kais붙여넣기'·'⏳ 계산 중…' 같은 형식 안내만 남김. 본건이 비면 복원. PR #91
- **배경 조회(사례 시군구 확인)는 수동 붙여넣기 단계(tryManual)를 쓰지 않는다.** 사용자가 누르지 않은 작업이 창을 띄우면 안 됨. 실패하면 조용히 대체값. PR #91
- **두 세션이 같은 파일의 같은 함수를 동시에 고치지 않는다.** 오늘 그 밖의 요인 `calcGongsi`를 두 세션이 건드려 충돌 위험. 영역을 나누거나, 먼저 끝난 쪽을 머지한 뒤 다음 세션에 "main 최신 반영" 지시.

## 2026-09-06 · 시점수정 계산기 (arm_시점수정.html)

- **시점수정 계산기는 탭 2단: 집합건물(주거용 | 비주거용) / 토지(지가변동률).** 처음엔 주거용·비주거용·토지를 한 줄에 나란히 뒀으나, 사용자가 물건 단위(집합건물/토지)로 먼저 나누길 원해 2단으로. 화면 이름도 '집합건물 시점수정'에서 '시점수정'으로. **위계가 보이게**: 최상위는 밑줄형 큰 탭 한 줄, 그 아래 작은 묶음(주거용 | 비주거용)을 왼쪽 정렬로. *기각*: 두 묶음을 같은 줄에 나란히(같은 급으로 보임), 하위 묶음 앞의 "집합건물 용도" 라벨과 세로선(사용자가 삭제 요청). PR #100 #101 #104 #105 용도지역·시군구를 R-ONE 표에서 접속 시 받아 드롭다운으로 고르고, 계산 규칙은 토지건물의 지가변동률 자동조회와 동일(첫·끝 달 일수비례, 미공표 월은 마지막 공표월 변동률로 일할 추정, 다섯째 자리 표시). 산식은 월별 변동률 한 줄씩 + 곱셈식으로 남겨 한글에 그대로 붙일 수 있게. 통계표 캐시 키는 토지건물과 공유(`tj-rone-jibyun-tbl`). *기각*: 지가변동률도 매매가격지수처럼 파일로 미리 받아두는 안 — 시군구×용도지역 조합이 많아 파일이 커지고, 프록시 조회가 이미 검증돼 있어 그대로 씀.
- **토지 탭 속도: 병렬 조회 + 하루 캐시.** 처음엔 한 달치씩 순서대로 물어 12개월이면 12번 이상 왕복(수 초~수십 초). 필요한 달을 한꺼번에(Promise.all) 받고, 통계표·최신 발표월·지역 목록과 월별 값(모든 지역·용도)을 브라우저에 하루 저장. 페이지 열 때 미리 준비. 두 번째부터는 통신 0회. 이후 사용자가 실사용 확인 뒤 **매일 파일로 미리 받아두는 안을 채택**(PR #107): `arap_launch.ps1`에 지가변동률 수신을 더해 `arap_jibyun_data.js`(최근 36개월, 전 지역·용도)를 매일 06시 Actions가 갱신. 계산기는 파일이 있으면 통신 0회, 없거나 파일 밖 오래된 달은 종전대로 프록시. 파일 크기 우려로 36개월만. Actions 수동 실행은 Claude 권한 밖(403) → 첫 실행은 사용자가 Actions 탭에서 jibyun_only 로. PR #102 #107
- **토지 지역 드롭다운은 전국 → 서울 → 경기 → 인천 → 나머지 시도 가나다, 시도 안에서는 '시도 전체' 다음 시군구 가나다순.** 사용자 요청("강남구가 제일 초반"). R-ONE 원래 순서(행정코드순)는 기각. 토지 계산 기록은 관리자 '접속' 탭 '내용' 칸에 `토지 주거지역 서울 강남구 · 거래→기준 = 시점수정치`로 남는다(별도 작업 불필요). PR #103
- **날짜칸은 숫자 8자리를 치면 2026.05.01 로 자동 정리 — 모든 앱 공통.** 사용자 결정("공통 기능으로 삼자"). 새 공통 파일 `arap_date.js`를 만들어 토지건물(기준시점·공시기준일·실지조사일·작성일자·의뢰일·사용승인일·거래사례 거래일자/사용승인일·평가사례 기준시점)과 시점수정 계산기(거래·기준시점)에 붙임. 집합건물·입주권은 이미 React `autoDateFmt`가 같은 일을 하고 있어 그대로 둠. 8자리 숫자가 금액칸과 겹치므로 **자동 감지가 아니라 `data-date`를 붙인 칸만** 바꾼다(*기각*: 모든 칸 자동 감지). 날짜로 안 읽히는 값(13월 등)은 건드리지 않음. PR #106
- **거래월이 최신 공표월보다 뒤여도 계산한다.** 거래월 이전의 마지막 공표월 변동률을 이어받아 일할 추정(토지건물은 이 경우 오류를 냈음 — 같은 규칙으로 맞출지는 보류).
- **이름 게이트 우회 가능성은 알고 있는 상태로 둔다.** 정적 사이트라 브라우저 안에서만 막는 구조이며, 개발자도구·localStorage·`?user=` 로 우회 가능. 사내 식별용으로는 충분하다고 보고 보강(시점수정 허용목록·수신함 검증·구글 로그인)은 **보류**. 광고(애드센스 등) 부착도 수익 대비 법인 이미지 부담으로 보류.

## 2026-09-07 · 토지 시점수정 — 부동산원 조회 화면과 값 맞추기

- **미공표 구간은 부동산원 방식으로 한 덩어리로 곱한다.** 기준시점이 최신 공표월(7월) 뒤면 `(1 + 7월 변동률 × 미공표 첫날~기준시점 일수 / 7월 일수)` 한 번만 곱함(예: 8.1~9.7 = 38/31). 종전엔 8월 한 달 복리 + 9월 7일 일할로 두 번 곱해 부동산원 화면(1.03820)과 다섯째 자리가 달랐음(1.03824). 공표 구간 누계도 부동산원처럼 소수 셋째 자리(%)로 반올림한 뒤 곱한다. *한계*: 월별 표시값(셋째 자리)로 누계를 내므로 부동산원 내부 누계(4.280%)와 0.001%p 차이가 남을 수 있음 → 다섯째 자리 ±1 차이 가능. R-ONE에 누계 항목이 있는지 확인은 보류(세션 환경에서 부동산원 접속 차단).
- **산식은 중간 반올림 없이 한 번에 곱한다.** 공표 구간 누계를 셋째 자리로 반올림한 뒤 곱하는 단계를 뺌(사용자 결정). 월별 `(1+r)` 곱 × 미공표 구간 `(1+r×일수/공표월 일수)`를 그대로 곱해 다섯째 자리로 표시. 부동산원과 마지막 자리 ±1 차이는 원자료(월별 셋째 자리 표시값) 정밀도 문제로 남겨 둠. PR #109

## 2026-09-07 · 집합건물 지분평가 — 출력물에 사정면적 반영

- **지분평가(1/2 등)·면적사정 건은 출력물의 '사정' 자리에 사정면적을 쓴다. 공부 자리는 그대로.** 앱 화면은 사정면적 75.33㎡로 맞았는데 한글 감정평가서 'Ⅲ. 감정평가액의 결정' 표와 명세표 엑셀 '사정' 칸이 공부 전유면적 150.65를 그대로 내던 것을 고침. 감정평가서 결정 표는 값을 사정면적으로, 머리글도 조정된 건이면 '전유면적'→'사정면적'(앱 화면과 같은 규칙). 'Ⅱ. 대상물건 개요'의 전유·공용·합계면적은 공부 그대로. 명세표는 H30(전유 사정)=사정면적, 지분평가면 H34(대지권 사정)에도 같은 지분을 곱함(조정 없으면 종전대로 `=+G30`/`=+G33` 수식 유지). 템플릿(base64)은 손대지 않고 앱이 치환 전에 그 표 안의 토큰만 바꾸는 방식 — 템플릿 재생성 스크립트 없이 안전하게.
- **괄호감정표·감정평가표 '평가내역' 사정 칸은 지분평가일 때만 '1세대 × 1/2'.** 공부 칸은 '1세대' 그대로, 면적사정(일부 제외)은 세대 표기를 바꾸지 않음(사용자: "지분평가일 경우에만"). 감정평가서 안의 감정평가표(`{{세대}}` 둘 중 두 번째), 별도 괄호감정표 hwpx와 한글2010용 hml('1세대' 고정 글자 둘 중 두 번째) 모두 같은 규칙. *한계*: 세션 환경에서 한글·엑셀 실행 불가 — XML 치환 결과와 openpyxl로 셀 값까지만 검증, 실제 열어 보는 확인은 사용자 몫.
- **사정면적 소수점 처리는 자리수 + 절사/반올림을 사용자가 고른다.** 지분평가·면적사정 모두 "소수점 N자리" 옆에 반올림/절사 선택(기본 반올림 = 종전 동작, 저장키 `areaRound`). 사용자 사례: 150.65 × 1/2 = 75.325 → 절사 75.32로 내고 싶음. 계산은 `roundArea()`로 통일하고 부동소수 오차(75.325가 75.32499…로 저장되는 문제)를 1e-7 보정 후 처리. 결과 옆에 "(소수점 2자리 절사)"를 표시해 어떤 규칙이 적용됐는지 보이게 함. 출력물(감정평가서·명세표)은 그 사정면적 값을 그대로 씀. 유효숫자 결정옵션(절사/반올림/없음)과 같은 용어 사용.

## 2026-09-07 · 토지건물 — 거래사례가 사라진 사고 → 저장 보호

- **원인 추정: 같은 화면을 두 탭에 띄운 채 옛 탭이 저장.** 저장이 "마지막에 쓴 탭이 이김"이었고 탭끼리 알림이 없어, 옛 탭에서 아무 칸이나 건드리면 그 탭의 옛(빈) 거래사례가 최신 저장을 덮음. 코드가 스스로 거래사례를 지우는 경로는 없음. (또 하나: 새 평가서에서 기존 접수번호를 치면 그 건이 소리 없이 덮임.)
- **저장마다 판 번호(`_rev`)를 붙이고, 저장소의 판이 이 탭이 아는 판과 다르면 덮어쓰지 않고 멈춘다.** 주황 안내줄로 「저장된 내용 불러오기 / 이 화면 내용으로 덮어쓰기」 중 고르게 함. 다른 탭이 저장하면 storage 이벤트로 즉시 표시. 새 평가서·건 열기 때는 '내 것' 기억을 비워 기존 접수번호 겹침도 같은 안내로 막음. *기각*: 다른 탭 내용을 자동으로 끌어오기 — 입력 중인 칸이 바뀌어 더 위험.
- **건별 자동 백업 「↺ 이전 버전」(최대 5개, `tojigeonmul-bak:접수번호`).** 덮어쓰기 직전 내용을 5분에 하나씩 보관하되, 거래사례·평가사례·표준지·토지가 **줄어드는 저장은 간격과 무관하게 보관**. 📂 저장된 건 목록에서 시각·요약(거래사례 n건…)을 보고 되돌림. 되돌리기 전 상태도 목록에 남아 다시 되돌릴 수 있음. 용량 부족이면 오래된 것부터 버림. 저장키 개명 없음(추가만).
- **여러 건 붙여넣기가 다음 열의 다른 사례를 덮으려 하면 확인창.** 취소하면 그 열은 두고 새 열에 추가. 창을 닫을 때 0.4초 대기 중이던 저장도 바로 씀. 집합건물·입주권(React)에는 아직 같은 보호가 없음 — **보류**(사용자 요청 시 같은 방식으로).

## 2026-09-08 · 집합건물 명세표 — 등기 전유부분에 부속면적(지하실 등)이 있는 건

- **등기 전유부분 건물내역이 두 줄(예: 철근콩크리트조 150.65㎡ / 지하실 9.09㎡)이면 명세표 전유 줄 아래에 부속면적 줄을 따로 낸다.** 사용자 실제 건(여의도 에이동 503호, 지분 1/2)의 수기 명세표를 그대로 규칙화: 지분평가면 31행 분자 `1` / 32행 `지하실 · 9.09 ×---- · 사정 4.54 · 공용면적` / 33행 분모 `2 · 포함` / 34행 빈 줄 / 35~37행 대지권(`16.80`, `15537 ×----1/2`, 사정 `8.40`). 지분이 아니면 31행 한 줄(`지하실 9.09 / 9.09`). 부속면적 사정은 사정면적과 같은 자리수·절사/반올림 옵션. 입력은 ① 물건정보 "부속면적(명세표용)" 명칭·면적 두 칸이고 등기 PDF 인식 때 자동 추출(annexName/annexArea, 전유면적에는 합산 금지). **부속면적은 명세표 표기에만 쓰고 단가·평가면적·감정평가서에는 넣지 않는다**(사용자 예시에서 감정평가액은 전유 줄에만 있음). 명칭(지하실)을 F열에 적는 것은 사용자 예시엔 없던 Claude 추가 — 어떤 면적인지 보이게 하려는 것, 빼달라면 뺀다.
- **지분평가 건은 대지권 산식 줄에 지분을 표기한다.** `15537 ×----1/2` (종전 `15537 ×----`). #110에서 대지권 사정면적만 지분을 곱했던 것을 표기까지 맞춤. 대지권 사정도 절사/반올림 옵션을 따름.
- *한계*: 세션 환경에서 엑셀 실행 불가 — 앱의 생성 함수를 node에서 그대로 돌려 셀 값·수식·행삽입·중괄호 도형 위치·인쇄영역까지 검증, 실제 열어 보는 확인은 사용자 몫. 집합건물_test.html은 #110 반영 전 옛 복사본이라 이번 명세표 변경을 넣지 않음.

## 2026-09-08 · 집합건물 다운로드 파일명

- **감정평가서 묶음 파일명은 `1. 감정평가서_의뢰인` / `2. 명세표_의뢰인` / `3. 요항표_의뢰인` 순.** 사용자가 정한 발송 순서(4. 광역위치도, 5. 현황사진은 앱에 없는 파일이라 앱 밖에서 이름만 맞춰 붙임). 종전 `3. 감정평가서`·`4.명세표`·`요항표(번호 없음)`. 표지청구서회보서(`1.`)·청구서 hwpx(`1. `)·괄호감정표(`2.`)·의견서 docx(`3. `)는 이 묶음에 대한 지시가 없어 그대로 둠 — 번호 충돌 정리는 사용자에게 확인 중.
- **명세표 템플릿을 사용자 새 양식(상단 로고·머리글 추가, 전체 4행 아래)으로 교체.** 사용자가 지하실 특이건으로 만든 파일을 그대로 받아 지하실 줄 2행(35·38행)을 빼고 H34·H38·G38을 원래 수식으로 되돌린 기본형을 내장. 앱의 칸 지정은 전부 +4행(소재지 C11~, 지번 D11~, 용도 E11~, 구조 F11~, 도로명 C17~, 층별 F17~G22, 필지 25행~, 건물란 31행, 전유 34행, 대지권 37~39행, 합계 48행). 끼워 넣는 행의 서식 번호도 새 파일 것으로. 검증: 지분+지하실 케이스를 생성하면 사용자 파일과 8~52행이 셀 단위로 일치(F36 명칭 추가·G36 문자열 표기만 다름), 기본 케이스는 템플릿과 동일.

## 2026-09-08 · 토지 시점수정 — 부동산원 조회 화면과 다섯째 자리까지 일치

- **부동산원 지가변동률 조회 화면의 계산 규칙을 그대로 따른다.** 화면 산식을 역산해 확인한 규칙: ① 첫 달 일할 `(1+r×일수/그 달 일수)` ② 그 해 1월 1일부터 포함되는 해는 월별 곱이 아니라 **그 해 누계**(예: 2026.01~07 → `(1+0.01354)`), 1월 1일부터가 아닌 해는 월별 `(1+r)` 곱 ③ 미공표 구간은 `(1+마지막 공표월 r×경과일수/공표월 일수)` 한 번. 누계는 월별 표시값(셋째 자리) 곱과 0.001%p 어긋나므로 **지가지수 표에서 누계 = 지수[해당월]/지수[전년 12월] − 1** 을 매일 수신 때 계산해 `arap_jibyun_data.js`의 `cum`에 담는다. 파일에 `cum`이 없으면(수신 실패·구판) 종전대로 월별 곱. 검증: 강남 주거 1.03820·상업 1.05121, 용인 수지 주거 1.02950 (부동산원 화면과 동일). *기각*: 월별 값으로 누계를 셋째 자리 반올림하는 안(PR #108, 상업 4.279 vs 4.280으로 불일치). PR #116
- **누계 수신은 Claude가 Actions를 못 돌리므로 사용자가 Actions 탭 → 지수 자동갱신 → jibyun_only 로 1회 실행해 확인.** 지가지수 표 이름·구조는 변동률 표와 같다고 보고 짰음(로그의 `[지가지수 표 후보]`·`연도 누계 N건 계산`으로 확인).

## 2026-09-08 · 건축물대장 조회 — 집합건물 한 동의 전체 호 전유면적

- **"전체 호를 한 번에 주는 API는 없다"는 절반만 맞다.** 전유공용면적(getBrExposPubuseAreaInfo)의 호 조건(hoNm)은 선택이라, 동만 주고 호를 빼면 그 동 모든 호의 전유·공용 행이 통째로 온다(한 페이지 최대 1,000행, 끝까지 이어 받음). 앱이 그 행을 호별로 묶어 전유 행만 합산하는 방식. 프록시 2판이 이미 동·페이지·행수를 넘기므로 프록시 재배포 없음. 단지 전체는 수십 페이지라 느리므로 **동 단위**로만(사용자 요청도 "동을 고른 뒤").
- **화면 표 + 엑셀 복사(2안) 채택, 파일 다운로드(1안) 기각.** 이유: 기존 한 호 복사와 같은 클립보드 방식이라 라이브러리 추가가 없고, 붙여넣기 전에 눈으로 확인 가능, CSV는 엑셀에서 한글 깨짐·xlsx는 외부 라이브러리 필요. 호 패널 아래 「이 동 전체 호 불러오기」 버튼 → 표(#·동명·호명·층·전유면적·용도, 360px 넘으면 스크롤, 「표 접기」) → 「전체 호 엑셀 복사」는 **동명·호명·층·전유면적** 4열만(사용자: "호이름-층-전유면적 이정도만"). 층은 지상은 숫자만(엑셀 정렬), 지하 `지1`, 옥탑 `옥1`. 전유가 여러 행(부속 지하실 등)이면 합산 — 한 호 요약과 같은 규칙. 전유부 호 목록엔 있는데 전유 행이 없는 호는 빨간 줄·빈 칸으로 끼워 넣어 빠진 것이 보이게. 동을 바꾸면 표를 비움.
- *한계*: 세션 환경에서 프록시·국토부 API 접속이 막혀 실제 대장으로는 못 돌려봄 — 가짜 응답(4페이지·다른 동 행·부속 전유·지하층·전유 행 없는 호)으로 Playwright 화면·클립보드 검증까지. 실제 건(홍제동 455 → 102동 113호)은 사용자 확인 몫.

## 2026-09-09 · 집합건물 앱·입장 화면 주소를 비공개 주소로 변경

- **집합건물 앱과 입장 화면(허브)의 주소를 무작위 이름으로 바꾸고, 새 주소는 관리자가 쓰게 할 사람에게만 직접 알려준다.** `집합건물.html`→`s3r86w8a.html`, `index.html`(허브)→`74w85izf.html`, 테스트 사본 `집합건물_test.html`→`s3r86w8a_test.html`(같은 접속코드로 열리는 사본이라 함께 옮김). 옛 주소는 안내 없이 404(사용자: 지우는 쪽). 저장된 건·이름·접속코드 통과 기록은 주소(도메인) 단위라 그대로 유지. 사용 목적은 기록하지 않음(사용자 요청).
- **루트 `index.html`은 시점수정 계산기 링크만 있는 공개 첫 화면으로 둔다.** 외부에 공유된 `시점수정.html`·`arm_시점수정.html` 주소는 그대로. 다른 화면의 🏠 홈 링크는 공개 첫 화면으로 가고, 집합건물 앱의 홈만 새 허브로 간다 — 입주권·토지건물 등 접속코드만으로 열리는 화면에서 허브 주소가 드러나지 않게. 같은 이유로 test.html(개발허브, 건축물대장에서 링크됨)의 집합건물 카드를 뺌.
- *기각/보류*: ① 접속코드 교체 + 이름 목록 제외(코드 공유로 뚫림) ② 1인 1코드 ③ Cloudflare Pages + Access 이메일 로그인(도메인 불필요·무료지만 이전 작업 필요) — 사용자가 "주소 변경"이 제일 간단하다고 결정. *한계*: 저장소가 공개라 github.com 파일 목록에서 새 이름이 보임. 막으려면 저장소 비공개(깃허브 Pages는 유료) 또는 Cloudflare Pages 이전 — 필요해지면 그때.
- **토지건물 지가변동률 자동조회도 같은 규칙(연도 누계·미공표 일할)으로 통일.** 성남 중원구 주거 26.01.01~07.21이 1.01359로 나와 부동산원(1.01360)과 어긋남 — 원인은 시점수정 계산기와 같음(월별 곱 1.2121% vs 누계 1.213%). `토지건물.html`이 `arap_jibyun_data.js`를 읽어 변동률·누계를 통신 없이 쓰고(표·최신월·지역·용도 목록도 파일에서), 파일 밖 오래된 달만 프록시. `jbFactor`는 arm_시점수정과 동일 로직(**수정 시 양쪽 함께**). 이미 저장된 사례의 시점수정치(timeMeta)는 자동 재계산하지 않음 — 「↻ 시점수정 자동」 또는 자동조회 버튼을 다시 눌러야 새 규칙 적용. PR #118
